# civic-addresses — tasks

- [ ] T1 Civic libs: `normalize-query`, `expand-street-variants`,
  `build-cql`, `tile-key` (AC-2.1, 2.3, 1.3) — unit tests per file.
- [ ] T2 WFS client with bbox loading + session cache (AC-1.3).
- [ ] T3 Map layer: red/black symbols at z≥17, click card, CC BY
  attribution (AC-1.1, 1.2, 3.1) — manual check.
- [ ] T4 Search island: official WFS lookup → Photon fallback, flyTo
  + coloured pin (AC-2.2, 2.4) — unit tests on pipeline, manual.
